# RS485Control 工程取舍

> 后续修改本组件的 agents 请继续维护本文档：有实际参数或工程权衡时记录，没有则不强行添加。

数据浏览的完整交互和聚合规则见 [`docs/DATA_BROWSER_DESIGN.md`](docs/DATA_BROWSER_DESIGN.md)。

- 两路 RS485 分别扫描 Modbus ID 1~247，子板不依赖 `[DeviceN]`；按 `portIndex + slaveId` 唯一，每口最多保留 16 块。空总线每轮探测 8 个地址，已发现子板后每轮探测 2 个；发现超时 150 ms，正常轮询仍用配置超时。
- 手动重新扫描会先停止自动温控并清空旧发现列表；高压告警期间拒绝重扫，避免清空告警来源后误解锁。快速发现后、首次完整电压数据回读前禁用手动 OT3/OT4。
- 高压联锁使用 `external_voltage / 10.0 > highVoltageThreshold`，默认 1.0 V；严格高于才触发，正式使用前需按现场传感器标定。
- 自动温控 OT3/OT4 状态改变默认最短间隔 10 秒，可设 1~3600 秒；该限制只抑制阈值附近的频繁切换，高压联锁切断不延时。
- SD 自动记录默认 1 秒；历史页首次进入才读取 CSV，后续切页复用内存缓存，后台新记录直接增量更新缓存。只有日期/子板条件改变后查询才重读，相同条件重复点击不读盘。查询后默认跟随最新，用户拖回旧数据时暂停跟随。水平滚动条高 36 px、拖块最小宽 100 px。
- 历史子板筛选合并当前发现列表和 CSV 中的端口/ID，离线旧子板仍可选。选择“全部子板”时按秒聚合：曲线显示子板平均温度及最高—最低范围，表格保留单板原始明细。CSV 子板索引只在进入历史页时重载，避免发现过程反复扫文件。
- 参数页使用紧凑顶部布局，操作反馈放在按钮之间；反馈显示 3 秒后自动消失，任一参数修改时立即清除旧反馈。SD 周期和继电器间隔使用 48×44 px、支持长按的外置 `− / +` 按钮。
- 主窗口最大化到桌面可用区域，最小尺寸 800×480，避免 XFCE 面板占用边缘后底部被截断。子板下拉列表最多显示 7 项，使用 36 px 触摸滚动条，离线项仍可选。
- 温湿度自检检查三路传感器完整性、有效量程和相对中位数偏差。默认初始化宽限 60 秒，温度有效范围 -40～85 ℃、最大偏差 15 ℃，湿度有效范围 0～100 %RH、最大偏差 30 %RH；异常时停止自动温控并切断 OT3/OT4，故障子板亮红灯，连续 3 次正常后恢复。阈值可在 app.ini 标定。
- 温控支持 threshold 与 pid 两种模式。PID 是适配机械继电器的两级输出：需求量低于 10% 全关、10～60% 开 OT3、60% 及以上开 OT3+OT4；默认 Kp=12、Ki=0.15、Kd=0，并继续服从继电器最短切换间隔。
- OT1/OT2/OT5/OT6 作为未定义备用输出，应用启动、发现子板、模式切换和告警时均强制关闭，且不提供用户操作入口。0x0002 备用输入只显示和记录，不参与联锁；硬件用途明确前不得赋予控制含义。
- 新 CSV 保持前五个身份字段兼容，同时增加温湿度/PT100 工程量、平均值、高压、备用输入、OT1～OT10、控制模式、目标温度和传感器质量列，末列继续保留完整 JSON。当天已有旧版 CSV 时继续按旧格式写到当天结束，避免混合表头。
- 参数页显示存储总量、剩余空间、日志占用、文件数和日期范围。手动清理严格删除所选日期之前的合法 YYYY-MM-DD.csv，先预览数量/大小再二次确认，不删除所选日期当天。
