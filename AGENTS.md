# RS485Control 工程取舍

> 后续修改本组件的 agents 请继续维护本文档：有实际参数或工程权衡时记录，没有则不强行添加。

数据浏览的完整交互和聚合规则见 [`docs/DATA_BROWSER_DESIGN.md`](docs/DATA_BROWSER_DESIGN.md)。

- 两路 RS485 分别扫描 Modbus ID 1~247，子板不依赖 `[DeviceN]`；按 `portIndex + slaveId` 唯一，每口最多保留 16 块。空总线每轮探测 8 个地址，已发现子板后每轮探测 2 个；发现超时 150 ms，正常轮询仍用配置超时。
- 手动重新扫描会先停止自动温控并清空旧发现列表；高压告警期间拒绝重扫，避免清空告警来源后误解锁。快速发现后、首次完整电压数据回读前禁用手动 OT3/OT4。
- 高压联锁可在参数页选择数字量或模拟量：数字量读取 `0x0001`，可配置 0 或 1 触发；模拟量读取 `0x0003`，原始值除以 10 后与 `highVoltageThreshold` 比较，严格高于才触发。默认保持模拟量模式和 1.0 V 代码默认值，避免升级后改变现场行为；正式使用前需按现场传感器标定。
- 自动温控 OT3/OT4 状态改变默认最短间隔 10 秒，可设 1~3600 秒；该限制只抑制阈值附近的频繁切换，高压联锁切断不延时。
- SD 自动记录默认 1 秒；历史页首次进入才读取 CSV，后续切页复用内存缓存，后台新记录直接增量更新缓存。只有日期/子板条件改变后查询才重读，相同条件重复点击不读盘。查询后默认跟随最新，用户拖回旧数据时暂停跟随。水平滚动条高 36 px、拖块最小宽 100 px。
- 历史子板筛选合并当前发现列表和 CSV 中的端口/ID，离线旧子板仍可选。选择“全部子板”时按秒聚合：曲线显示子板平均温度及最高—最低范围，表格保留单板原始明细。CSV 子板索引只在进入历史页时重载，避免发现过程反复扫文件。
- 参数页使用紧凑顶部布局，主页只保留温控参数；计算说明默认折叠，避免小屏被长文案占用，点击“展开计算说明”查看。参数输入框和下拉框禁用鼠标滚轮，避免触摸屏/鼠标误改数值或选项。高压联锁、存储、备用接口和继电器项收纳在“高级设置”，入口放在“重新扫描子板”旁，减少常用页面滚动。操作反馈放在按钮之间；反馈显示 3 秒后自动消失，任一参数修改时立即清除旧反馈。温控参数只显示当前 threshold 或 PID 模式相关项，控制模式下拉与其他枚举选项统一使用自绘蓝底白字高亮；高压阈值独立为“高压安全联锁”卡片，不随温控模式切换。SD 周期、日志删除时长和继电器间隔使用支持长按的外置 `− / +` 按钮。
- 主窗口最大化到桌面可用区域，最小尺寸 800×480，避免 XFCE 面板占用边缘后底部被截断。子板下拉列表最多显示 7 项，使用 36 px 触摸滚动条，离线项仍可选。
- 显示主题收纳在“高级设置”，只提供“标准 / 低光 / 强光”三种按现场光线命名的预设，选择后即时生效并单独持久化；告警红、运行绿等安全语义颜色不随主题改变，避免把外观偏好误解为设备状态。历史曲线的背景、坐标与十字线跟随主题调色板，数据序列颜色保持固定，便于跨主题识别。
- 温湿度自检不比较三路偏差，也不将缺数据作为故障；仅对固定极端量程（温度 -40～85 ℃、湿度 0～100 %RH）外的读数报警。自检为非阻塞通知，不停止整体自动温控、不拦截手动 OT3/OT4。自动调度会排除超出固定量程的温度值，用剩余正常探头的平均温度继续调度；三路均无正常值时才跳过该子板本轮调度。顶部固定显示 60 秒自检倒计时，故障后连续 3 次正常恢复并显示剩余次数，不向用户开放这些参数。
- 温控目标默认跟随每块子板内部两路有效 PT100 的平均温度，也可改为固定温度；PT100 采用与外部温度相同的 -40～85 ℃控制有效范围，有一路有效即可跟随，两路均无有效值时跳过该子板本轮调度。外部三路有效温湿度温度的平均值仍是被控量。
- 温控支持 threshold、pid、dew_point 三种前端可选模式。阈值模式也按两个规格不同的加热回路映射为“低功率单路、高功率单路、两路同开”三档，默认温差点 0.3/1.5/3.0 ℃、切档回差 0.2 ℃。PID 默认输出点 10%/35%/60%、Kp=12、Ki=0.15、Kd=0。两种模式共用默认一级单路 OT3 的功率顺序，单路顺序和各自三个输出点均可在前端调整，并继续服从继电器最短切换间隔。PID 参数预览假设“此前稳定在目标、积分为 0，1 秒内突发降温”，反算刚好跨过三个输出点的温度，并显示各边界的 P/I/D 贡献。
- 防凝露是 threshold/pid 之外的第三种独立控制模式：三组有效温湿度分别按 Magnus 公式计算露点取最高值，与内部有效 PT100 平均温度相减得露点余量，按余量三档切档（默认 3/2/1 ℃、回差 0.2 ℃），无最高档限制；外部平均温度超过 PT100 平均值 + 温升上限（默认 5 ℃）时停止加热。选择该模式时参数页隐藏目标来源与阈值/PID 参数，防凝露余量/回差/温升上限和“两个单路档位顺序”直接并入“温度控制”卡片显示；档位顺序与阈值/PID 模式共用同一配置，两处下拉双向同步。
- OT1/OT2/OT5/OT6 各自在前端配置为安全关闭、手动控制、跟随自动、跟随报警、跟随 OT3 或跟随 OT4；跟随主回路时优先同步本次下发值，没有主回路指令时按每块子板的当前状态同步。首次发现时手动输出初始化为关。0x0002 备用输入可在前端配置为仅监视、高电平联锁或低电平联锁。所有业务逻辑参数由前端保存并应用，不要求客户编辑配置文件。
- 新 CSV 只保存身份、三路温湿度、两路 PT100、外部电压、备用输入和 OT1～OT6；不保存可推导平均值、设定参数、指示灯或冗余 JSON。当天已有旧版 CSV 时继续按旧格式写到当天结束，避免混合表头。
- 参数页显示存储总量、剩余空间、日志占用、文件数和日期范围。日志容量上限默认 12 GB，上限内全部保留，不再按天数自动删除。手动清理按“多少天/月/年以前”计算截止日期并显示日期参考，仍只删除截止日期之前的合法 YYYY-MM-DD.csv，先预览数量/大小再二次确认。
