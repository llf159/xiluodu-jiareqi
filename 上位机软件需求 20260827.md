# 上位机软件需求

## 接线定义

### 输出端口

1. `OT01`：备用。
2. `OT02`：备用。
3. `OT03`：接四路继电器板的第一路。
4. `OT04`：接四路继电器板的第二路。
5. `OT05`：备用。
6. `OT06`：备用。
7. `OT07`：接三色灯的绿色。
8. `OT08`：接三色灯的黄色。
9. `OT09`：接三色灯的红色。
10. `OT10`：接蜂鸣器。

### 输入端口

高压带电传感器的输出接入电控板的“输入”信号引脚。

> **注意：低电平信号通断有效！**

### RS485 接口与设备地址

1. 第一组 RS485：接入上位机主控板（ID 可调节）。
2. 第二组 RS485：外接三路温湿度传感器，采用菊花链接法。
3. 第三组 RS485：外接两路 PT100 温度传感器。
4. 板载 4 位拨码开关可以调节本身 ID 通迅地址编号。

> **注意：重新上电后生效。**

## 软件逻辑

上电 1 分钟内，循环读取所有子板的温湿度数据。如果有数据异常，直接跳出报警提示并点亮相应子板的红灯；如果都正常，则进入下一步。

### 1. 高压带电信号处理

当“输入”引脚的高压带电信号有效时，上位机主控板发指令停掉所有子板的 `OT3` 与 `OT4`，同时所有子板都亮起红色灯，蜂鸣器发出提示声。

在手动或自动操作下，打开 `OT3` 或 `OT4` 的动作均无效。

> **此传感器优先级最高。**

### 2. 主要界面

上位机触摸屏包含以下四个主要界面：

1. 手动操作
2. 自动界面
3. 参数设置
4. 数据浏览

### 3. 手动操作

选择“手动操作”界面后，可以在屏上任意选择相应的子板，显示当前的温湿度值、输入信号状态等信息，同时可以通过触控切换 `IO3` 与 `IO4` 的输出变化。

此时所有子板的黄色灯亮，代表进入手动操作状态。

### 4. 自动操作

选择“自动操作”界面后，可以在屏上任意选择相应的子板，显示当前的温湿度值、目前温湿度值、输入信号状态等信息，同时放置“启动”和“停止”按键。

- 点击“启动”键后，子板的绿灯点亮，程序开始自动运行。
- 点击“停止”键后，子板的绿灯熄灭，程序停止。

程序自动运行时，通过温度与湿度的阀值自动切换 `OT3` 与 `OT4` 的输出变化，具体规则见下方。

### 5. 参数调置

选择“参数调置”界面后，允许修改并保存以下参数值。

> **注意：** 原来规划温度与湿度独立控制，但加热器内部已经把风扇与加热丝并联在一起，下面主要讨论温度阀值（湿度独立控制后期再说）！

温度控制一种用 PID 算法，一种用固定阀值法。为简化，在此讨论阀值法。

#### 5.1 温度控制参数

| 参数 | 参数值 | 单位 |
| --- | --- | --- |
| 温度目标值 | `XX.X` | 度 |
| 温度下限回差值 | `YY.Y` | 度 |
| 温度上限回差值 | `ZZ.Z` | 度 |

设三路温湿度传感器的平均温度值为 `T`：

| 条件 | OT3 | OT4 |
| --- | --- | --- |
| `T < (XX.X - YY.Y)` | 打开 | 打开 |
| `T > XX.X` | 打开 | 关闭 |
| `T > (XX.X + ZZ.Z)` | 关闭 | 关闭 |

#### 5.2 SD 卡自动记录周期

- 记录周期：`BBB` 秒。
- 每隔指令周期，自动将读取到的子板温湿度数据保存在 SD 卡里，便于回溯。

### 6. 数据浏览

选择“数据浏览”界面后，允许回看以往的历史记录。历史记录可以采用数字或曲线图形式，表达时间与温度、湿度的关系。
